# Environment Configuration Template
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# APPLICATION SETTINGS

# Base URL for the application being tested
# Default: https://automationexercise.com
BASE_URL=https://automationexercise.com

# Environment: development, staging, or production
# Default: development
NODE_ENV=development

# PLAYWRIGHT CONFIGURATION

# Number of parallel test workers
# Default: 1 (for test isolation)
# Set to higher for faster execution
WORKERS=1

# Overall test timeout in milliseconds
# Default: 35000 (35 seconds)
TIMEOUT=35000

# Assertion/expect timeout in milliseconds
# Default: 12000 (12 seconds)
EXPECT_TIMEOUT=12000

# Navigation timeout in milliseconds
# Default: 20000 (20 seconds)
NAVIGATION_TIMEOUT=20000

# LOGGING CONFIGURATION

# Log level: DEBUG, INFO, WARN, ERROR, CRITICAL
# Default: INFO
LOG_LEVEL=INFO

# Directory for log output
# Default: test-results/logs
LOG_DIR=test-results/logs

# PERFORMANCE & OPTIMIZATION

# Turbo Mode: true or false
# When enabled, blocks images and fonts for 3x speed improvement
# Useful for smoke tests and CI/CD
# Default: false
TURBO_MODE=false

# Number of test retries on failure
# Default: 2
RETRIES=2

# CI/CD DETECTION (Usually set automatically)

# Set to 'true' when running in CI environment
# Usually set automatically by CI system
# CI=false

# GitHub Actions indicator
# Usually set automatically by GitHub Actions
# GITHUB_ACTIONS=false

# TEST CREDENTIALS (Optional - for test user scenarios)

# Test user email for account creation/login tests
# Optional: Leave empty if not needed
# TEST_EMAIL=testuser@example.com

# Test user password for account creation/login tests
# Optional: Leave empty if not needed
# TEST_PASSWORD=SecurePassword123!

# API CONFIGURATION (Optional)

# API key for backend API testing
# Optional: Leave empty if not needed
# API_KEY=your_api_key_here

# API authentication token
# Optional: Leave empty if not needed
# API_TOKEN=your_api_token_here

# JIRA INTEGRATION (Optional)

# Jira instance base URL
# Example: https://your-org.atlassian.net
# Required for Jira reporter
# JIRA_BASE_URL=https://your-org.atlassian.net

# Jira user email for API authentication
# Required for Jira reporter
# JIRA_EMAIL=your-email@company.com

# Jira API token (NOT your password)
# 🔐 SENSITIVE: Keep this secret
# Generate at: https://id.atlassian.com/manage-profile/security/api-tokens
# JIRA_API_TOKEN=your_api_token_here

# Jira project key where bugs will be created
# Example: QA, TEST, PROJ
# Default: QA
# JIRA_PROJECT_KEY=QA

# Enable automatic Jira issue creation on test failure
# Set to 'true' in CI to enable
# Default: false
JIRA_REPORTER_ENABLED=false

# EMAIL CONFIGURATION (Optional)

# SMTP server hostname for email testing
# Examples: smtp.gmail.com (Gmail), smtp.sendgrid.net (SendGrid)
# Optional: Leave empty if not needed
# SMTP_HOST=smtp.gmail.com

# SMTP server port
# Common ports: 587 (TLS), 465 (SSL), 25 (plain)
# Optional: Leave empty if not needed
# SMTP_PORT=587

# SMTP authentication username (usually email address)
# Optional: Leave empty if not needed
# SMTP_USER=your_email@gmail.com

# SMTP authentication password or app-specific password
# ⚠️  SENSITIVE: Use app passwords for Gmail, not account password
# Generate at: https://myaccount.google.com/apppasswords
# Optional: Leave empty if not needed
# SMTP_PASSWORD=xxxx xxxx xxxx xxxx

# NOTIFICATIONS (Optional)

# Slack webhook URL for test failure notifications
# 📖 Setup guide: https://api.slack.com/messaging/webhooks
# Generate at: https://api.slack.com/apps → Create New App → Incoming Webhooks
# Format: https://hooks.slack.com/services/T00000000/B00000000/XXXXXXX
# Optional: Leave empty if not needed
# SLACK_WEBHOOK=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Enable/disable notifications on test completion
# Default: false
# Set to 'true' to enable notifications
NOTIFICATION_ENABLED=false

# REPORT PUBLISHING (Optional - for GitHub Pages)

# GitHub personal access token (PAT) for publishing test reports
# 🔐 SENSITIVE: Keep this secret - never commit or expose
# Create at: https://github.com/settings/tokens
# Scopes needed: repo, workflow, pages
# Create fine-grained token at: https://github.com/settings/tokens?type=beta
# Optional: Leave empty if not needed
# GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Public URL where test reports are automatically published
# Example: https://username.github.io/repo-name/reports
# This is typically GitHub Pages URL or custom domain
# Optional: Leave empty if not needed
# REPORT_URL=https://example.github.io/reports

# DEPLOYMENT & ENVIRONMENTS (Optional)

# Staging environment base URL (for pre-production testing)
# Optional: Leave empty to use production BASE_URL
# STAGING_URL=https://staging.automationexercise.com

# Production environment base URL override
# Optional: Leave empty to use primary BASE_URL
# PRODUCTION_URL=https://automationexercise.com

# ADVANCED CONFIGURATION (Optional - Expert Users Only)

# Debug mode: Set to 'true' for maximum logging verbosity
# Useful for troubleshooting test failures
# Default: false
# DEBUG=false

# Headless mode: Set to 'false' to run browser in headed mode
# Useful for debugging test execution visually
# Default: true (recommended for CI/CD)
# HEADLESS=true

# Screenshot on failure
# Automatically capture screenshots when tests fail
# Default: true
# SCREENSHOT_ON_FAILURE=true

# Video on failure
# Record video only for failed test runs (saves bandwidth)
# Default: false
# VIDEO_ON_FAILURE=false

# Test data directory
# Location of test fixtures and data files
# Default: fixtures/test-data
# TEST_DATA_DIR=fixtures/test-data

# PERFORMANCE TUNING (Optional)

# Network condition simulation
# Options: slow-4g, fast-4g, slow-3g, fast-3g, offline
# Simulate network latency for performance testing
# Optional: Leave empty for normal network conditions
# NETWORK_CONDITION=fast-4g

# Device emulation profile
# Options: iPhone 12, Pixel 5, iPad, etc.
# Optional: Leave empty for desktop
# DEVICE_PROFILE=iPhone 12

# CUSTOM/PROJECT-SPECIFIC VARIABLES

# Add any project-specific secrets or configuration below
# Examples:
# CUSTOM_API_ENDPOINT=https://api.example.com
# CUSTOM_AUTH_TOKEN=your_custom_token
# PROJECT_ID=12345

# IMPORTANT NOTES

# 1. NEVER commit .env to version control
#    - It's in .gitignore for a reason!
#    - Contains sensitive credentials

# 2. For local development:
#    - Copy this file to .env
#    - Fill in your values
#    - Run: npm test

# 3. For CI/CD (GitHub Actions):
#    - Set values as GitHub Secrets
#    - They're automatically injected as environment variables
#    - See .github/workflows/test.yml for details

# 4. Priority order (.env.local > .env > process.env):
#    - .env.local is checked first (allows local overrides)
#    - .env is used if .env.local doesn't exist
#    - process.env (system) is fallback

# 5. Type validation:
#    - All values are validated using Zod schema
#    - Invalid values will cause startup failure
#    - Check error messages for required values

# QUICK START

# Minimum setup for local testing:
# 1. Uncomment BASE_URL (or use default)
# 2. Uncomment WORKERS if running parallel tests
# 3. Run: npm test

# Full setup for production-like testing:
# 1. Set all APPLICATION SETTINGS
# 2. Set all PLAYWRIGHT CONFIGURATION
# 3. Set LOGGING CONFIGURATION as needed
# 4. Add TEST CREDENTIALS if needed
# 5. Configure notifications/reports (optional)


